# ==========================================
# GenTwin Full-Stack Environment Variables
# ==========================================

# --- FRONTEND (React / Vite) ---
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_BASE_URL=ws://localhost:8000/ws

# --- BACKEND (FastAPI - Reference) ---
# Note: Currently, backend config is stored purely in config.py,
# but you can use these keys if you choose to dockerize or wrap the ports.
PORT=8000
HOST=0.0.0.0
